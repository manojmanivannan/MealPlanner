# MealPlanner

## Agent skills

### Issue tracker

Issues and PRDs live in this repo's GitHub Issues (github.com/manojmanivannan/MealPlanner), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles map to identically-named GitHub labels; `wontfix` already exists on the repo. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.